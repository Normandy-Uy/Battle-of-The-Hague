<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          xmlns:tools="http://schemas.android.com/tools">

  <!-- Explicit for Play Console Advertising ID declaration (AdMob / Android 13+). -->

  <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

  <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" />

  <application android:appCategory="game">

    <!--

      Must keep MAIN/LAUNCHER here. Custom activity enables EdgeToEdge (Android 15+).

      appCategory=game + resizeableActivity=true address Play large-screen recommendations.

    -->

    <activity android:name="com.unity3d.player.UnityPlayerActivity" tools:node="remove" />

    <activity

        android:name="com.dutz.battleofthehague.DutzUnityPlayerActivity"

        android:exported="true"

        android:launchMode="singleTask"

        android:resizeableActivity="true"

        android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"

        android:hardwareAccelerated="true"

        android:theme="@style/UnityThemeSelector"

        tools:replace="android:launchMode,android:resizeableActivity,android:exported,android:theme">

      <intent-filter>

        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />

      </intent-filter>

      <meta-data android:name="unityplayer.UnityActivity" android:value="true" />

    </activity>

  </application>

</manifest>

